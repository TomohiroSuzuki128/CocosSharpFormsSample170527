<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
			 prism:ViewModelLocator.AutowireViewModel="True"
			 Title="関東地方"
			 x:Class="CocosSharpFormsSample.Views.MainPage">
	<Grid x:Name="MainGrid" RowSpacing="0">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<StackLayout Grid.Column="0" Grid.Row="1">
			<StackLayout Padding="10,50,10,00" Margin="0,0,0,0">
				<Button HeightRequest="25" BackgroundColor="Gray" TextColor="White" Text="群馬県" Command="{Binding GunmaCommand}" />
				<Button HeightRequest="25" BackgroundColor="Gray" TextColor="White" Text="栃木県" Command="{Binding TochigiCommand}" />
				<Button HeightRequest="25" BackgroundColor="Gray" TextColor="White" Text="茨城県" Command="{Binding IbarakiCommand}" />
				<Button HeightRequest="25" BackgroundColor="Gray" TextColor="White" Text="千葉県" Command="{Binding ChibaCommand}" />
				<Button HeightRequest="25" BackgroundColor="Gray" TextColor="White" Text="埼玉県" Command="{Binding SaitamaCommand}" />
				<Button HeightRequest="25" BackgroundColor="Gray" TextColor="White" Text="神奈川県" Command="{Binding KanagawaCommand}" />
				<Button HeightRequest="25" BackgroundColor="Gray" TextColor="White" Text="東京都" Command="{Binding TokyoCommand}" />
			</StackLayout>
		</StackLayout>
	</Grid>
</ContentPage>